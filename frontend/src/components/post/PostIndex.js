import React from 'react';
import withStyles from '@mui/styles/withStyles';

const styles = () => ({});

const PostIndex = () => {

  return (
    <div>
      Hi
    </div>
  );
};

export default withStyles(styles)(PostIndex);
